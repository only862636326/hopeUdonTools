



# HopeUdonUnity

cmd 型
   ls
   cls
   clear
   help
   history
   time
   vars
   pwd

goto 相关

   goto lable
   if var goto lable
   ifgoto lable

loop:

   
endloop;

cmd parameter 型
   cd par
   addroot par
   lable line30


变量 
   var = value 型
   var 型
   .var 获取udon数值, 挂在物体上第一个脚本













